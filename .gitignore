# Directory BUILD
build/
_build/
_local_build

# Directory SAVES
saves/
tests/
__pycache__/

# others
*.o
*.d
*.tar.gz
*.sha256
*.spv
*.key
identity.hash
*.save

# Directory RPM
rpmbuild/
_local_build/
_srpm_test/
build-tests/

# ignore
npcs.dat
world.dat
artifacts.dat
# The combat log can grow to tens/hundreds of MB (a single long session
# produced 92 MB) and its rotation file <name>.1 is just as big:
# NEITHER must ever reach a git tag / source tarball / RPM (L6).
combat_log.json
combat_log.json.1
local_check.sh

# dev artifacts produced by the DM commands (dm_pdf / dm_mapfloor)
map_dump*.txt
map_floor*.pdf
